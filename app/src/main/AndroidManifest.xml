<?xml version="1.0" encoding="utf-8"?>
<manifest
    package="com.keshogroup.cartrader"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-sdk
        android:minSdkVersion="16"/>

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="false"
        android:enabled="true"
        android:icon="@drawable/ic_launcher_autotrader"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.Light.DialogWhenLarge">

        <activity
            android:name=".controller.CartraderActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>

        <service
            android:name=".controller.CartraderService"
            android:icon="@drawable/circle"/>

    </application>

</manifest>
